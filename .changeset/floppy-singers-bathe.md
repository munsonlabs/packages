---
'@munsonlabs/video-player': patch
---

Custom elements, CSS classes and custom properties now read vendor, package, then part, so `<ml-controls-play-button>` is `<ml-video-play-button>` and `--mlv-accent` is `--ml-video-accent`.

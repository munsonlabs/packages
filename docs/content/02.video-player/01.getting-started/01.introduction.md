---
title: Introduction
description: What @munsonlabs/video-player is, the three ways to use it, and live demos of both.
navigation:
  icon: i-lucide:book-open
---

`@munsonlabs/video-player` plays MP4/HLS/DASH, YouTube, Vimeo, Dailymotion, Brightcove, and JW Player behind one component API, with Google IMA ads and Prebid.js header bidding. Standalone - it shares no code with anything else in this repo.

## Ways to use it

| Form           | What you get                                                                                                                         |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| Vue 3          | `VideoPlayer`, `VideoCard`, `VideoStage`, `VideoPlaceholder`, 13 headless controls - or `app.use(VideoPlayerPlugin)` for all of them |
| Web components | The same source as `<ml-video-*>` and `<ml-controls-*>` elements, for any framework or none                                          |
| No-build / CDN | Import the `/element` bundle via an import map and drop tags into HTML                                                               |

Three bundles - `/element` (everything), `/element/core` (player tags), `/element/controls` (controls only) - each with a matching `/style*` entry point, so you ship only the CSS you render.

## See it running

Every example in these docs is live, and built from this repo's source - so what you see always matches the code in the same commit:

::player-example{src="https://test-streams.mux.dev/x36xhzz/x36xhzz.m3u8" title="Big Buck Bunny" action="mute" caption="Live VideoCard, built from this repo's source."}
::

## The published package

The examples above are built from source. This one is the published npm artifact instead - the `/element` web-component bundle, fetched from a CDN at page load, wired up exactly as the [web component docs](/video-player/core-concepts/web-components) describe:

::video-player-demo
::

## Where to start

:::card-group
::card
---

title: Installation
icon: i-lucide:download
to: /video-player/getting-started/installation
---

Install, mount a video, read the `state-change` stream.
::

::card
---

title: Components
icon: i-lucide:component
to: /video-player/core-concepts/components
---

The four components and which one to reach for.
::

::card
---

title: Platforms
icon: i-lucide:globe
to: /video-player/core-concepts/platforms
---

Every source it plays, and how detection works.
::

::card
---

title: Web components
icon: i-lucide:box
to: /video-player/core-concepts/web-components
---

The `ml-*` bundles, and loading from a CDN.
::

::card
---

title: API reference
icon: i-lucide:book-marked
to: /video-player/api
---

Props, methods & state, events, headless controls.
::

::card
---

title: Features
icon: i-lucide:layers
to: /video-player/features/viewport
---

Viewport, stage & playlists, transcripts, captions, quality, ads, theming.
::
:::

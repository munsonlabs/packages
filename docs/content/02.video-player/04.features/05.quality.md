---
title: Quality
description: Adaptive bitrate quality selection for HLS and DASH.
navigation:
  icon: i-lucide:gauge
---

Quality selection needs real variant streams, so it applies to adaptive HLS and DASH on the native `<video>` path. YouTube, Vimeo, and Dailymotion expose no quality override - they manage it themselves inside their iframe. Brightcove and JW Player get it wherever their resolved source is adaptive. Plain MP4 has no variants, so no Quality menu.

## Adaptive HLS / DASH

A Quality option appears on its own once hls.js or dash.js has parsed the manifest's resolution variants - nothing to switch on:

```vue
<VideoCard src="https://devstreaming-cdn.apple.com/videos/streaming/examples/img_bipbop_adv_example_fmp4/master.m3u8" />
```

::player-example{src="https://devstreaming-cdn.apple.com/videos/streaming/examples/img_bipbop_adv_example_fmp4/master.m3u8" label="bipbop" poster="https://img.youtube.com/vi/aqz-KE-bpKQ/0.jpg" caption="Apple's bipbop stream declares several resolution variants - open the controls and Quality lists them, starting on Auto."}
::

## Selecting a level

The control cycles Auto → highest → … → lowest → Auto. **Auto leaves the streaming engine's adaptive bitrate algorithm in control**, which is the right default for most viewers - picking a level pins it and disables ABR until you restore Auto.

Programmatically, `setQuality(index)` selects a level and `null` restores Auto, firing `qualitychange` - on your own calls and on the engine's own ABR switches alike. Levels are exposed as `{ index, height, bitrate, label }`.

## Asking for a height

Indices are the engine's own and differ from one manifest to the next, so to carry a choice across sources use the `quality` prop instead: a target height in pixels - the 720 in 720p - and the player picks the nearest level each source actually offers, as soon as its levels are known. `null` (or unset) leaves Auto in charge. It's one-way, like `playbackRate`: a level the viewer picks by hand stands until the prop changes.

```vue
<VideoCard :src="src" :quality="720" />
```

The `qualitychange` event carries `qualityHeight` alongside `qualityIndex` for the same reason - remember the height, feed it back through the prop, and a viewer's pick follows them from video to video. The [shorts feed](/video-player/examples/shorts-feed) does exactly this.

The list is one rung per resolution, ascending, not one per manifest variant. A ladder usually carries each resolution several times over at different bitrates and codecs - bipbop above lists 1080p nine times - so each height is represented by its highest-bitrate variant, and audio-only renditions are left out. `index` stays the engine's own, so it's still what `setQuality` takes.

::warning
Safari plays HLS natively, and native HLS offers no manual quality-override hook - so the Quality menu won't appear there, while [captions](/video-player/features/captions) still work.
::
